// src/lib/sanity.ts
// Sanity CMS tidak aktif — data website saat ini menggunakan hardcoded static data.
// File ini dipertahankan sebagai placeholder untuk integrasi Sanity di masa depan.

export const sanityClient = null

export const urlFor = (_source: unknown) => ({ url: () => '' })

// ─── GROQ Queries (placeholder) ──────────────────────────────────────────────

export const galleryQuery = ``
export const postsQuery = ``
export const postBySlugQuery = ``
export const testimonialsQuery = ``
export const clientsQuery = ``
